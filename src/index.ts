/**
 * brother-ql-webusb — print to Brother QL / P-touch label printers directly
 * from the browser using WebUSB.
 *
 * This is a TypeScript port of the `brother_ql` Python package (kept in this
 * repository under `brother_ql/`, documented in PYTHON.md). The raster protocol
 * encoding is verified byte-for-byte against the Python implementation through
 * committed golden fixtures.
 */

export const VERSION = '0.1.0';
